# Supabase Edge Functions

Reservado para tareas que deban ejecutarse específicamente en el runtime Edge de Supabase. La API principal de dominio vive en Fastify; no duplicar lógica entre Edge Functions y Fastify.

Toda excepción debe documentarse mediante ADR y contar con tests/observabilidad equivalentes.
